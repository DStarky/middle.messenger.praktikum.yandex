.simple-input-container {
  position: relative;
}

.simple-input {
  width: 100%;
  background-color: var(--gray-input);
  padding: 10px 35px 10px 35px;
  border: none;
  outline: none;
  border-radius: 5px;
  color: var(--gray-color);
  font-weight: var(--base-font-weight);
  font-size: var(--base-font-size);
  line-height: var(--base-line-height);
}

.simple-input::placeholder {
  font-size: 12px;
  color: var(--chat-item-message);
  line-height: var(--base-line-height);
  font-weight: var(--base-font-weight);
  transition: opacity 0.3s ease;
}

.simple-input_placeholder-center::placeholder {
  text-align: center;
}

.simple-input_icon {
  position: absolute;
  top: 50%;
  left: 35%;
  transform: translateY(-50%);
  pointer-events: none;
  transition: left 0.2s ease;
}

.simple-input:focus::placeholder {
  opacity: 0;
}

.simple-input:focus + .simple-input_icon {
  left: 10px;
}

.simple-input:not(:placeholder-shown) + .simple-input_icon {
  left: 10px;
}

.simple-input_message {
  border-radius: 9999px;
  padding: 10px 20px;
  font-size: 12px;
  line-height: var(--base-line-height);
  font-weight: var(--base-font-weight);
  color: var(--gray-color);
}

.simple-input_message::placeholder {
  font-size: 12px;
  line-height: var(--base-line-height);
  font-weight: var(--base-font-weight);
}
